{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Depurar analizador.exe (C++14)",
            "type": "cppvsdbg",                   
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe", 
            "args": [ "Tetris.brik" ],            
            "stopAtEntry": false,
            "cwd": "${fileDirname}",              
            "console": "integratedTerminal",
            "preLaunchTask": "C/C++: cl.exe build active file"
        }
    ]
}
